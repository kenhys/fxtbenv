// Disable auto update feature
lockPref("app.update.auto", false);
lockPref("app.update.enabled", false);
lockPref("app.update.url", "");
lockPref("app.update.url.override", "");
lockPref("browser.search.update", false);

// Enable browser/extensions/*.xpi
lockPref("extensions.autoDisableScopes", 11);

// Always show firstrun page to confirm current version
lockPref("browser.startup.homepage_override.mstone", "");

// Skip default browser check (needs combination)
lockPref("browser.shell.didSkipDefaultBrowserCheckOnFirstRun", false);
lockPref("browser.shell.skipDefaultBrowserCheckOnFirstRun", true);

// Enable browser toolbox
lockPref("devtools.chrome.enabled", true);

// Enable remote debugger
lockPref("devtools.debugger.remote-enabled", true);

// Enable globalChrome.css debugging
lockPref("extensions.globalchromecss@clear-code.com.debug", true);

